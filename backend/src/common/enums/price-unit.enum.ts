/** Fiyatın hangi birim üzerinden tanımlandığı. */
export enum PriceUnit {
  PER_PLATE = 'per_plate', // Plaka başına
  PER_M2 = 'per_m2', // Metrekare başına
  PER_KG = 'per_kg', // Kilogram başına
}
